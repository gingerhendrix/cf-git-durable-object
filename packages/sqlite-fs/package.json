{
  "name": "sqlite-fs",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --outdir=dist --format=esm",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vitest": "^3.1.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
